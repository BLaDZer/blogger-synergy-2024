{% extends 'layouts/app' %}

{% block content %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">{{ __('Create post') }}</div>

                    <div class="card-body">
                        <form method="POST" action="{{ route('posts.new') }}">
                            <input type="hidden" name="_token" value="{{ csrf_token() }}" />

                            <div class="form-group">
                                <label for="message" class="col-md-4 col-form-label">{{ __('Message') }}</label>

                                <div class="col-md-8">
                                    <textarea id="message" class="form-control form-control-lg" name="message" required autofocus></textarea>
                                </div>
                            </div>

                            <div class="form-group">
                                <div class="col-md-8">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" value="1" id="is_hidden" name="is_hidden">

                                        <label class="form-check-label" for="is_hidden">
                                            {{ __('Content is visible after request') }}
                                        </label>
                                    </div>
                                </div>
                            </div>

                            <div class="form-group mb-0">
                                <div class="col-md-4">
                                    <button type="submit" class="btn btn-primary">
                                        {{ __('Create') }}
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
